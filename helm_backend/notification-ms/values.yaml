replicaCount: 1

image:
  repository: lian1280/ceramicraft-notification-mservice
  tag: dev-202604031155
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080
  grpcPort: 50051

livenessProbe:
  path: /notification-ms/v1/ping
  initialDelaySeconds: 30
  periodSeconds: 10
readinessProbe:
  path: /notification-ms/v1/ping
  initialDelaySeconds: 30
  periodSeconds: 10

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

volumes:
  configMapName: notification-ms-config
  logPath: /tmp/logs/notification-ms
