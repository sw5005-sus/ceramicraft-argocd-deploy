apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: product-agent-external-secret
  namespace: default
spec:
  refreshInterval: "5m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: product-agent-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: secret/ceramicraft/product_agent_config
    - extract:
        key: secret/ceramicraft
