apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-ms-config
data: {}
  # notification-ms is configured entirely via environment variables injected
  # through the Vault external secret (ms-secret-vault).
  # See .env.example in ceramicraft-notification-mservice for the full list.
