apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mlflow

resources:
  - ../../base            
  - db-endpoints.yaml       

commonLabels:
  env: prod-ish 

patches:
  - target:
      kind: Deployment
      name: mlflow
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1